{
  "name": "gravitationallensing",
  "private": true,
  "scripts": {
    "serve": "bun --hot index.html",
    "server": "bun run server.js",
    "dev": "bun run server.js & bun --hot index.html"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "three": "^0.174.0"
  }
}